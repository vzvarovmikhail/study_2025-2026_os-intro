---
author: Взваров Михаил
date: 2026
institute: РУДН, НПИбд-01-25
lang: ru-RU
subtitle: Поиск файлов. Перенаправление ввода-вывода. Просмотр
  запущенных процессов
title: Лабораторная работа №6
---


# Цель работы

Ознакомиться с инструментами поиска файлов и фильтрации текстовых
данных, приобрести навыки управления процессами и анализа использования
диска.

---

# Задание

-   Записать имена файлов из '/etc' и домашнего каталога.
-   Найти '.conf'-файлы.
-   Выполнить поиск файлов по имени.
-   Запустить процесс в фоновом режиме.
-   Запустить и завершить 'gedit'.
-   Изучить 'df' и 'du'.

---

# Перенаправление вывода

    ls /etc > file.txt
    ls ~ >> file.txt

Операторы:

-   '>' — перезаписывает файл;
-   '>>' — добавляет данные в конец;
-   '2>' — перенаправляет поток ошибок.

---

# Фильтрация данных

    grep "\.conf" file.txt
    grep "\.conf" file.txt > conf.txt

'grep' использовался для поиска строк по заданному шаблону.

---

# Поиск файлов

    find ~ -maxdepth 1 -name "c*" -type f 2>/dev/null
    find /etc -maxdepth 1 -name "h*" -type f 2>/dev/null

Команда 'find' позволяет выполнять поиск по имени и типу объекта.

---

# Фоновые процессы

    find /var/log -name "log*" -type f \
      2>/dev/null > ~/logfile &
    
    jobs

Символ '&' запускает команду в фоновом режиме.

---

# Использование диска

    df -h
    du -sh ~/

-   'df' показывает использование файловых систем.
-   'du' показывает объём, занимаемый файлами и каталогами.

---

# Поиск каталогов

    find ~ -type d 2>/dev/null
    find ~ -maxdepth 1 -type d 2>/dev/null

Опция '-type d' ограничивает поиск каталогами.

---

# Выводы

В ходе работы были освоены перенаправление потоков, 'grep', 'find',
фоновые процессы и команды 'df' и 'du'.

Получены практические навыки работы с файлами, процессами и дисковым
пространством из командной строки Linux.

Цель работы достигнута.

---

# Контрольные вопросы

**1. Какие стандартные потоки существуют?**
'stdin (0)' — ввод, 'stdout (1)' — вывод, 'stderr (2)' — ошибки.

**2. Чем '>' отличается от '>>'?**
'>' перезаписывает файл, '>>' добавляет данные в конец.

**3. Что такое конвейер?**
Конвейер '|' передаёт вывод одной команды на ввод другой.

**4. Для чего используются 'find' и 'grep'?**
'find' ищет файлы и каталоги по критериям, 'grep' фильтрует текст по
шаблону.

**5. Для чего нужны 'df' и 'du'?**
'df' показывает использование файловых систем, 'du' — размер файлов и
каталогов.

---

# Библиография

1.  Методические указания к лабораторной работе.
2.  Linux man pages: 'find', 'grep', 'ps', 'kill', 'df', 'du', 'top'.
3.  GNU Coreutils Manual.
